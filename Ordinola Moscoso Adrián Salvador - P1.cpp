//https://github.com/ADRIAN-SALVADOR-ORDINOLA-MOSCOSO-323/2da-Prac.Calificada-23200323
//Práctica Calificada 2 - Fila D 
/*
// Ejercicio 1:
#include <iostream>
using namespace std;

int main() {
    const int filas = 3;
    const int columnas = 3;
    int matriz[filas][columnas];
    int suma_filas[filas] = {0};
    int suma_columnas[columnas] = {0};

    cout << "Introduce los elementos de la matriz 3x3:" << endl;
    for (int fila = 0; fila < filas; ++fila) {
        for (int columna = 0; columna < columnas; ++columna) {
            cout << "Elemento [" << fila << "][" << columna << "]: ";
            cin >> matriz[fila][columna];
        }
    }

    for (int fila = 0; fila < filas; ++fila) {
        for (int columna = 0; columna < columnas; ++columna) {
            suma_filas[fila] += matriz[fila][columna];
            suma_columnas[columna] += matriz[fila][columna];
        }
    }

    cout << "Suma de las filas:" << endl;
    for (int fila = 0; fila < filas; ++fila) {
        cout << "Fila " << fila << ": " << suma_filas[fila] << endl;
    }
    cout << "Suma de las columnas:" << endl;
    for (int columna = 0; columna < columnas; ++columna) {
        cout << "Columna " << columna << ": " << suma_columnas[columna] << endl;
    }

    return 0;
}
*/

